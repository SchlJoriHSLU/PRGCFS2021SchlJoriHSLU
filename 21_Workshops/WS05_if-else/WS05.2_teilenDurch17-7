#include<stdio.h>

int main()

{
	int start = 1;
	int end = 200;	
	
	for( int i = start; i <= end; i++)
		{
		if( i%17==0) 
		printf( "17: %d\n", i);
		
		if( i%7 == 0 && i%11 == 0)
		printf( "7 and 11: %d\n", i );
		}
	
	return 0;
}
